REACT_APP_API_BASE=http://localhost:5000
# Include leading slash if needed; set to empty string if backend has no /api prefix
REACT_APP_API_PREFIX=/api
REACT_APP_WITH_CREDENTIALS=true

# If backend already sets CORS to allow credentials and you use session cookies, keep true.
# Set to false if using stateless JWT in Authorization header instead.

# Troubleshooting login Failed to fetch:
# 1. Ensure backend running at REACT_APP_API_BASE (curl http://localhost:8080/actuator/health)
# 2. If backend path differs, adjust REACT_APP_API_PREFIX accordingly.
# 3. If using package.json proxy, you can comment out REACT_APP_API_BASE to let CRA proxy.
# 4. For HTTPS mismatch (frontend https, backend http), browsers may block mixed content.
