# Used only by `npm test` (Jest). Not a secret -- tests never make real network
# calls to this address, apiClient.js just needs a truthy value to not throw at
# import time. Committed so `npm test` works the same for everyone.
REACT_APP_API_BASE=http://localhost:5000
